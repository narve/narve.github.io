{
  "name": "narve-github-io",
  "private": true,
  "scripts": {
    "build": "eleventy && pagefind --site _site",
    "build:preview": "INCLUDE_FUTURE=1 eleventy && pagefind --site _site",
    "serve": "eleventy --serve",
    "serve:preview": "INCLUDE_FUTURE=1 eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.2",
    "node-html-parser": "^6.1.13",
    "pagefind": "^1.1.0"
  }
}
